# AGENT-PLATFORM-EXEC-01C5B2 — Checkpoint

**Task ID:** AGENT-PLATFORM-EXEC-01C5B2
**Title:** AI-Service Harness entitlement proof verification and worker enforcement
**Phase:** INDEPENDENT_SECURITY_CONSOLIDATION
**Result:** PASS
**Date:** 2026-09-06
**Reviewer:** Independent (Opus 4.6)
**Nature:** 3-step IMPLEMENTATION — Step 3 independent security consolidation

---

## 1. Implementation range

**Base SHA:** `4422a9330aed04e7fcf4275625fd725978d30eea`
**Implementation SHA:** `cecbea5ced70ac1e0aff5ed00b0bfa8890a74ca3`

### Exactly one commit:

1. `cecbea5ced70ac1e0aff5ed00b0bfa8890a74ca3` — `feat: verify Harness entitlement proofs in worker`

### Exactly four files changed:

1. `services/ai-service/src/queue/job.types.ts`
2. `services/ai-service/src/worker/worker.processor.ts`
3. `services/ai-service/src/worker/worker.processor.spec.ts`
4. `services/ai-service/src/worker/__tests__/worker.processor.builder-config.spec.ts`

---

## 2. Security findings

### No CRITICAL, HIGH, or MEDIUM findings.

All findings from the predecessor EXEC-01C5B1 checkpoint (HIGH-001 prototype pollution, MEDIUM-001 secret-read ordering) were addressed in the Gateway producer and the AI-Service consumer independently reimplements the same defenses:

- `sortKeysRecursive` uses `Object.create(null)` at every object level — no inherited `__proto__` setter invocation.
- Secret is read and validated (missing/empty/whitespace → fail closed) before HMAC computation.
- Signature and payloadDigest are validated as 64-char lowercase hex before `Buffer.from`/`timingSafeEqual`.

### INFORMATIONAL-001 — ESLint configuration absent

**Severity:** INFORMATIONAL
**Detail:** `services/ai-service/.eslintrc*` and `eslint.config*` do not exist. `npm run lint` fails with "ESLint couldn't find a configuration file." This is a pre-existing condition, not introduced by this commit.

---

## 3. Queue-contract audit

| Contract item | Status |
|---|---|
| `HarnessEntitlementProof` has exactly eight frozen fields | ✅ `version`, `executionId`, `userId`, `apiKeyId`, `harnessVersion`, `issuedAt`, `payloadDigest`, `signature` |
| `version` narrowed to literal `1` | ✅ `readonly version: 1` |
| `harnessVersion` narrowed to literal `'v1'` | ✅ `readonly harnessVersion: 'v1'` |
| `AiExecutionJob.harnessEntitlementProof` is optional | ✅ `harnessEntitlementProof?: HarnessEntitlementProof` |
| Ordinary producers and fixtures remain source-compatible | ✅ builder-config spec creates `AiExecutionJob` without proof; compiles without error |
| No proof defaults, invented proof, or implicit trust flag | ✅ Proof field is purely optional; no default value |
| No unrelated queue-contract change | ✅ Only `HarnessEntitlementProof` interface and the optional field added |

---

## 4. Canonicalization and prototype-sensitive-key proof

Independent reproduction (Node.js `crypto`, no imports from implementation):

| Property | Result |
|---|---|
| Only `harnessEntitlementProof` excluded from digest | ✅ |
| Every other job field covered (new field changes digest) | ✅ |
| `JSON.stringify` → `JSON.parse` normalization matches BullMQ | ✅ |
| Object keys sort recursively with `.sort()` | ✅ |
| Arrays retain order | ✅ |
| Every constructed object has null prototype | ✅ |
| Own `__proto__` survives (via JSON round-trip) | ✅ |
| Nested own `__proto__` survives | ✅ |
| Changing any covered value changes digest | ✅ (all 7 base fields + model + adapter) |
| Input objects not mutated | ✅ |
| SHA-256 over UTF-8 bytes, lowercase hexadecimal | ✅ |
| Serialization failures cannot escape as raw errors | ✅ (`computePayloadDigest` wraps in typed error) |
| No collisions across test payloads | ✅ |

---

## 5. Golden vector independent recomputation

Using only Node.js `crypto.createHash` and `crypto.createHmac` (no implementation imports):

- Canonical JSON: byte-identical ✅
- Payload digest: `8b58d2d281263357f70c8a489e3bbf4e32e0facfb5db0e83cd522b62e007188a` ✅
- Claim string: `v=1|executionId=exec-golden-01|userId=user-golden-01|apiKeyId=apikey-golden-01|harnessVersion=v1|issuedAt=2026-09-05T12:00:00.000Z|payloadDigest=8b58d2...` ✅
- HMAC-SHA256 signature: `d247e17634be269b0bbef1eb843a65bf299935ea840853900fe5dda3d8ef11b5` ✅
- Tests do not import Gateway implementation ✅
- Expected constants are hardcoded frozen values, not generated by functions under test ✅

---

## 6. Shape-validation and error-code matrix

| Input | Expected code | Result |
|---|---|---|
| Missing proof (v1 job, no proof) | `HARNESS_ENTITLEMENT_PROOF_MISSING` | ✅ |
| `null` proof | `HARNESS_ENTITLEMENT_PROOF_MISSING` | ✅ |
| Boolean proof (`true`) | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| Array proof (`[]`) | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| Empty object proof (`{}`) | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| String version `"1"` | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| Numeric version `2` | `HARNESS_ENTITLEMENT_PROOF_UNSUPPORTED_VERSION` | ✅ |
| Empty required strings | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| Wrong proof harnessVersion `"v2"` | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| Non-hex digest | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| Uppercase signature | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| Wrong-length signature | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| Non-hex signature | `HARNESS_ENTITLEMENT_PROOF_MALFORMED` | ✅ |
| Correct shape, incorrect signature | `HARNESS_ENTITLEMENT_PROOF_INVALID_SIGNATURE` | ✅ |

Buffer lengths and hex form validated before `timingSafeEqual` (confirmed by spy showing no `timingSafeEqual` call for malformed/uppercase/wrong-length inputs).

---

## 7. Identity and payload-substitution analysis

| Substitution | Expected code | Result |
|---|---|---|
| `executionId` changed | `BINDING_MISMATCH` | ✅ |
| `userId` changed | `BINDING_MISMATCH` | ✅ |
| `apiKeyId` changed | `BINDING_MISMATCH` | ✅ |
| `harnessVersion` changed | `MALFORMED` (proof `harnessVersion` checked as structural) | ✅ |
| Prompt changed | `PAYLOAD_INTEGRITY_MISMATCH` | ✅ |
| Session ID changed | `PAYLOAD_INTEGRITY_MISMATCH` | ✅ |
| Provider changed | `PAYLOAD_INTEGRITY_MISMATCH` | ✅ |
| Model added after signing | `PAYLOAD_INTEGRITY_MISMATCH` | ✅ |
| Own `__proto__` added | `PAYLOAD_INTEGRITY_MISMATCH` | ✅ |
| Nested `__proto__` added | `PAYLOAD_INTEGRITY_MISMATCH` | ✅ |
| Adapter changed | `PAYLOAD_INTEGRITY_MISMATCH` | ✅ (covered by all-field-changes test) |
| New unknown field added | `PAYLOAD_INTEGRITY_MISMATCH` | ✅ |
| Wrong secret | `INVALID_SIGNATURE` | ✅ |

Trust is never derived from `agentId`, `sessionId`, prompt contents, API-key naming, `isInternal`, tool-loop flag, or provider/adapter identity — confirmed by source inspection.

---

## 8. HMAC and timing-safe comparison

| Check | Result |
|---|---|
| Secret key name exactly `HARNESS_ENTITLEMENT_HMAC_SECRET` | ✅ |
| Worker normalization matches locked Gateway producer (§8.3.2) | ✅ (identical `sortKeysRecursive` with `Object.create(null)`) |
| Missing secret → `SECRET_NOT_CONFIGURED` | ✅ |
| Empty secret → `SECRET_NOT_CONFIGURED` | ✅ |
| Whitespace-only secret → `SECRET_NOT_CONFIGURED` | ✅ |
| HMAC algorithm is SHA-256 | ✅ (`createHmac('sha256', secret)`) |
| Expected signature recomputed from frozen claim | ✅ |
| `timingSafeEqual` compares equal-length 32-byte buffers | ✅ (verified by spy) |
| Wrong secret fails | ✅ |
| No timing comparison for malformed signature input | ✅ (spy confirms no `timingSafeEqual` call) |
| Crypto/buffer exceptions become typed safe failures | ✅ (catch → `INVALID_SIGNATURE`) |
| Secret, signature, proof, digest, canonical payload, prompt absent from logs/errors | ✅ (test assertions confirm) |

---

## 9. Worker guard placement

Verification occurs AFTER:
- Pending-to-running ledger claim (line ~970)
- Cancellation pre-check (line ~1052)

Verification occurs BEFORE:
- Harness routing (`resolveHarnessRouting` — line ~1197)
- Route/config evaluation logs (line ~1204)
- Adapter resolution (line ~1192)
- Dispatcher construction (line ~1270)
- Tool-handler registration (lines ~1276-1334)
- Tool advertisement (`requireNonEmptyAdvertisedHarnessTools` — line ~1335)
- Provider execution
- `executeAgentHarnessLoop` (line ~1372)
- Harness audit events
- Checkpoint creation

Confirmed by line-number trace of `worker.processor.ts`: entitlement verification at line 1156-1187, before all downstream operations.

---

## 10. Failure finalization and retry analysis

| Check | Result |
|---|---|
| `HarnessEntitlementError.name` = `'HarnessEntitlementError'` | ✅ |
| `HarnessEntitlementError.isRetryable` = `false` | ✅ |
| Safe deterministic message (no secret/signature/prompt) | ✅ |
| Every verifier failure crosses boundary as typed error | ✅ (catch rethrows or wraps) |
| Invalid proof reaches existing failure finalizer | ✅ (outer catch → `SET execution_status='failed'`) |
| Ends with ledger state `failed` | ✅ |
| Records only safe error information | ✅ |
| Does not enter provider retry loop | ✅ (`isRetryableError` won't match entitlement codes) |
| Does not call provider | ✅ (confirmed by `execute.not.toHaveBeenCalled()` test) |
| Does not invoke Harness loop | ✅ |
| Emits no Harness audit event | ✅ |
| Creates no checkpoint | ✅ |
| Does not advertise or dispatch tools | ✅ |
| BullMQ retry: `attempts: 1` at Gateway prevents job-level retry | ✅ |
| Worker `isRetryableError` does not match entitlement error patterns | ✅ |

---

## 11. Ordinary-job compatibility and non-activation

| Check | Result |
|---|---|
| Ordinary jobs work without proof | ✅ (`harnessVersion !== 'v1'` → skip) |
| Ordinary jobs work without HMAC secret | ✅ (secret not read for non-v1 jobs) |
| Existing single-shot execution unchanged | ✅ |
| Worker identity/final-metadata propagation unchanged | ✅ |
| Builder configuration unchanged | ✅ (builder-config spec: 37 tests pass) |
| Existing unsupported-provider/disabled-gate behavior intact | ✅ |
| Gateway producer files unchanged in this range | ✅ |
| No frontend change | ✅ |
| No provider-adapter change | ✅ |
| No transcript or tool-loop change | ✅ |
| No credit/accounting change | ✅ |
| No Harness flag change | ✅ |
| No product-visible activation | ✅ |
| No migration or dependency change | ✅ |

---

## 12. Test results

### Full AI-Service Jest suite

| Metric | Value |
|---|---|
| Test suites | 38 passed, 0 failed |
| Tests | 903 passed, 1 skipped, 0 failed |
| New failures | 0 |

### TypeScript build

`npm run build` — `tsc` — **PASS** — zero errors.

### ESLint

**UNAVAILABLE** — no ESLint configuration file exists in `services/ai-service/`. Pre-existing condition, not introduced by this commit.

### `git diff --check`

Clean.

### Lane-capacity validator

**PASS** — `idleCode=NO_PAIRWISE_ADMISSIBLE_CANDIDATE` — working tree clean — proof written only to `$env:TEMP`.

---

## 13. End state

| Item | End state |
|---|---|
| EXEC-01C5B2 | COMPLETE AND LOCKED — PASS |
| Candidate status | LOCKED |
| Lane 1 | EMPTY |
| Lane 2 | EMPTY |
| Lane 3 | DISABLED |
| GATEWAY | UNOWNED |
| ENV | UNOWNED |
| AI-SERVICE | UNOWNED |
| GOVERNANCE | UNOWNED |
| `HARNESS_ENTITLEMENT_PROOF_V1` | FROZEN |
| Parent EXEC-01C5B | READY / NOT ADMITTED / PROVISIONAL / `admissionUncertain=true` |
| Parent Step 3 implementation children | COMPLETE (EXEC-01C5B1 LOCKED + EXEC-01C5B2 LOCKED) |
| Parent Step 4 final verification/lock | NOT STARTED |
| EXEC-01C6 | NOT REGISTERED |
| Runtime authorization | all false |
| Product-visible Harness | FUTURE / gated |
| Harness flags | false |
| Git commit | NO |
| Git push | NO |

---

## 14. Dirty paths (expected)

1. `C:\Users\knlee\aiSandBox2026B\TASKS.md`
2. `C:\Users\knlee\aiSandBox2026B\TASKS_BACKLOG_FULL.md`
3. `C:\Users\knlee\aiSandBox2026B\docs\control-plane\lane-saturation-state.json`
4. `C:\Users\knlee\aiSandBox2026B\docs\AGENT-PLATFORM-EXEC-01C5B2-CHECKPOINT.md`

---

*Checkpoint created: 2026-09-06 — AGENT-PLATFORM-EXEC-01C5B2 — independent security consolidation — PASS.*
