/**
 * User role enumeration
 * - admin: Full system access
 * - user: Standard user access
 * - beta: Beta testing users with special features
 */
export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  BETA = 'beta',
}
